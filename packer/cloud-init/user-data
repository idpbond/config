#cloud-config
ssh_pwauth: true

users:
  - name: packer
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    lock_passwd: false
    plain_text_passwd: packer

growpart:
  mode: auto
  devices: [/]

package_update: false
package_upgrade: false
